wifi:
  networks:
    - ssid: !secret ssid1
      password: !secret ssid1_password
    - ssid: !secret ssid2
      password: !secret ssid2_password
  use_address: roode.local
  fast_connect: True
  power_save_mode: none
